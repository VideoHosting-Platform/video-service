# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
values.yaml.example
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~
# Various IDEs
.project
.idea/
*.tmproj
.vscode/

# Исключить архивы зависимостей
*.tgz
charts/*.tgz

# Исключить системные файлы
.git/
.idea/
*.log
tmp/

# Исключить большие файлы
*.zip
*.tar.gz
